/**
 * RueIV Mega v2 — RH-style layout (minimal JS)
 * ------------------------------------------------
 * Hover preview and brand carousel removed.
 * This file is retained for future enhancements only.
 * The dropdown open/close is handled by the theme's
 * native <details is="details-mega"> custom element.
 */
(function () {
  'use strict';
  // No JS needed — CSS-only layout.
  // Kept as a shell to avoid 404 on the asset reference.
})();
